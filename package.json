{
  "name": "crypto-price-tracker-worker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./index.js --bundle --target=esnext --format=esm --platform=node --outdir=dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.2"
  }
}
